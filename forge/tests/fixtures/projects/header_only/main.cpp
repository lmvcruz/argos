#include <iostream>
#include "template_utils.h"

int main() {
    std::cout << "Max(10, 20) = " << max(10, 20) << std::endl;
    std::cout << "Min(10, 20) = " << min(10, 20) << std::endl;
    return 0;
}
